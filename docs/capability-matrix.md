# 읽기/쓰기 가능·불가 매트릭스

> 진행하면서 계속 갱신하는 살아있는 문서. 새 항목을 검증할 때마다 여기에 반영한다.
> 최종 수정: 2026-09-09

**범례:** ✅ 됨(검증) · ⚠️ 부분/불확실 · 🔜 예정(구조는 준비) · ❌ 막힘 · ⛔ 범위 밖

| # | 대상 (데이터) | 오프라인 읽기 | 오프라인 쓰기 | 근거 / 비고 |
|---|---|:---:|:---:|---|
| 1 | 패키지 헤더 (magic·버전) | ✅ | 🔜 | 고정 레이아웃. 라운드트립 시 유지 |
| 2 | Name표 (모든 이름 문자열) | ✅ | ⚠️ | 164개 클린 파싱. 쓰기는 인덱스 재계산 필요 |
| 3 | Import표 (외부 클래스·에셋 참조) | ✅ | ⚠️ | 49개, 객체/클래스/패키지 해석 |
| 4 | Export표 (내부 오브젝트 + serial 영역) | ✅ | 🔜 | 22개, `serial_offset/size` = 쓰기 주소 |
| 5 | 배치 디바이스 인벤토리 (클래스·개수) | ✅ | ⛔ | export→import 참조로 정밀 클래스. `VerseDevice_C`도 잡힘 |
| 5b | 레벨 전체 인벤토리 + CLI 리포트 | ✅ | ⛔ | `python -m uefn_inspector <경로>`. PointLevel 67액터 |
| 5c | 프로젝트 인덱스 + 참조 그래프 | ✅ | ⛔ | build_index/build_reference_graph (M1) |
| 5d | 검색(search) · 역참조(where-used) | ✅ | ⛔ | 지난 세션 '찾기' 토일 해소 (M1) |
| 5e | 디바이스 설정 **발견**(이름·enum) | ✅ | ❌값 | list_settings — 설정 존재/이름만, 값은 9번 천장 |
| 6 | 참조 에셋 (사운드·메시 경로) | ✅ | ⚠️ | `/` 경로 추출. Heartbeat 3단 확인 |
| 7 | 트랜스폼 (위치·회전·스케일) | 🔜 | 🔜 | 표준 태그드 프로퍼티. 디코더로 예정 |
| 8 | 표준 태그드 프로퍼티 (Bool/Int/Float/Enum) | ⚠️ | 🔜 | 이름·타입은 보임. 값 디코드는 부분 |
| 9 | 디바이스 설정값 `Can Be Heard By` | ❌ | ❌ | GUID 낀 **비표준(Verse-VM)** 직렬화 |
| 10 | `@editable` 바인딩 (KnifeDesign 등) | ❌ | ❌ | Verse VM 내부 데이터. 리플렉션도 실패 |
| 11 | Verse 로직 (`.verse` 의미 해석) | ⛔ | ⛔ | 범위 밖 (참조만) |
| 12 | 라이브 조작 (스폰·컴파일·GUI 토글) | — | — | 기본 MCP(에디터) 몫 |

## 경계 요약

- **읽기(오프라인)**: 구조·참조·인벤토리는 ✅. 표준 프로퍼티 값은 ⚠️(진행). Verse-VM 설정값은 ❌.
- **쓰기(오프라인)**: 지금 미구현. 구조상 표준 프로퍼티 편집 + 라운드트립은 🔜 가능. Verse-VM(9·10)은 구조로도 못 넘는 **천장** → 라이브 MCP 몫.
- **핵심 가치**: 기본 MCP가 못 하는 **오프라인·병렬 분석**. "값 하나까지"가 아니라 "무엇이 어디 배치·구성됐는지 지도".

## 변경 이력
- 2026-09-09: 초안. 항목 1~6 ✅ 검증(AudioPlayer 실파일 + PointLevel 67파일). 9·10 ❌ 확정(GUID 직렬화 관찰).
- 2026-09-09: 레벨 인벤토리 + CLI(5b) ✅. VerseDevice_C 탐지 확인. TDD 7 tests green.

> 참고: 테스트 픽스처(`tests/fixtures/`)는 사용자 MyProject에서 복사한 UEFN 콘텐츠라 git 미포함(.gitignore). 로컬에서만 재현.
